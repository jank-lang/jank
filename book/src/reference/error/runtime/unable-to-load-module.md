# runtime/unable-to-load-module
This happens when jank finds a module but fails while loading or initializing it.

## Mitigations
Read the accompanying error details closely. Then fix the exception or nested
error that occurred during module loading.
