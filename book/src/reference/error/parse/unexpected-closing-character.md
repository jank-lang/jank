# parse/unexpected-closing-character
Unexpected closing character.

## Additional explanation
This happens when `)`, `]`, or `}` appears without a matching opening delimiter.

## Mitigations
Remove the extra closing delimiter, or add the matching opening delimiter earlier in the form.
