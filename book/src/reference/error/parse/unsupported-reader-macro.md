# parse/unsupported-reader-macro
Unsupported reader macro.

## Additional explanation
This happens when `#` is followed by a reader macro that jank does not support.

## Mitigations
Use a supported reader macro, or rewrite the form without that reader syntax.
