# analyze/invalid-cpp-type
Invalid C++ type.

## Additional explanation
This happens when a form that requires a C++ type is malformed or is given a
value form instead of a type form.

## Mitigations
Pass a valid C++ type form in that position.
