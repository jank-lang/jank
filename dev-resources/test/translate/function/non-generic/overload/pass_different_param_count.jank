(ƒ overloaded () ()
  (print "nullary"))

(ƒ overloaded (d integer) ()
  (print "integer"))

(ƒ overloaded (d integer dd real) ()
  (print "integer and real"))

(overloaded 42 3.14)
(overloaded -77)
(overloaded)
