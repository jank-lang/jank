(declare hash-map : (key ∀ value ∀))
(declare hash-map : (key ∀ value ∀))
(declare hash-map : (key ∀ value ∀))
(declare hash-map : (key ∀ value ∀))
(declare hash-map : (key ∀ value ∀))

(λ (o hash-map : (∀ ∀)) (hash-map : (∀ ∀))
  o)
