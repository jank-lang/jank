(declare test-integer ƒ : ((integer) ()))

(declare test-real ƒ : ((real) ()))

(declare test-string ƒ : ((string) ()))

(bind test
  (λ (a integer b real c string) ()
    (test-integer a)
    (test-real b)
    (test-string c)))

(test 77 -5.05 "good")
