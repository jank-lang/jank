<program> = atom*
<atom> = string / number / boolean /
         qualified-keyword / keyword / identifier /
         binding-definition

integer = #'[+-]?\d+'
real = #'[+-]?\d+\.\d*'
<number> = (integer | real)

<double-quote> = '\"'
string = <double-quote> #'(?:[^"\\]|\\.)*' <double-quote>

<identifier-text> = #'[^\s"()\d:][^\s"()/]*'
identifier = identifier-text
qualified-keyword = #'::?' identifier-text '/' identifier-text
keyword = #'::?' identifier-text

boolean = 'true' | 'false'

<left-paren> = <'('>
<right-paren> = <')'>

<def-keyword> = <'def'>
binding-definition = left-paren def-keyword identifier atom right-paren
