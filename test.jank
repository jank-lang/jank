(func foo (i integer) ()
  (print "called with integer: " i))
(func foo (a integer b integer) ()
  (print "called with integers: " a " " b))
(func foo (b boolean) ()
  (print "called with boolean: " b))

(foo 42)
(foo 42 43)
(foo true)
