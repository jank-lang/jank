refactoring
  expect state

syntax errors...?
  will the regex remain?
    need to map s-exprs to file positions

idents
  allow numbers
  allow underscores

use long names
  var => variable
  let => where

members
  global member name with . prefix
  (struct coord
    (x int)
    (y int))

  (.x my_coord)
  (.y my_coord)

  Swizzling with tuples?
    (.x.y my_coord)

current list
  should be a tuple
  list should be a generic heterogenous container

allow unicode for specials
  function => ƒ
  lambda => λ

variadic generics
  working with parameter packs

Maybes?
  pattern matching
    replace car, cdr, cond, eq, atom
