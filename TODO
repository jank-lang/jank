refactoring
  expect state

cons, lists
  built-in sum types?

use null in the appropriate areas

constants

syntax errors...?
  will the regex remain?
    need to map s-exprs to file positions

members
  Swizzling with tuples?
    (.x.y my_coord)

current list
  should be a tuple
  list should be a generic heterogenous container

variadic generics
  working with parameter packs

lambdas
  lambda => λ
  function syntax is?
    (function : ((A1 A2...) (R1 R2...)))

Maybes?
  pattern matching
    replace car, cdr, cond, eq, atom
