* Todos
  - ( ) Handle main-run that runs the `-main` function in namespace configured in project.clj


* Handle main-run
  - Jank's configuration will be stored in a `:jank` key in lein project map.
  - To get correct classpath, we need to have all the paths i.e. 
  -- `source-paths`
  -- `test-paths`
  -- `resource-paths`
  -- `compile-path`
  -- `dependencies`
