{:tasks {build  (do (shell "cmake -Bbuild -DCMAKE_BUILD_TYPE=Release")
                    (shell "cmake --build build"))
         server {:depends [build]
                 :task    (shell "./build/jank-nrepl-server" (clojure.string/join " " *command-line-args*))}}}
