;; Verifies that recur works when the target loop is INSIDE the catch block.
(assert
 (= :success
    (try
      (throw :trigger)
      (catch cpp/jank.runtime.object_ref _
        (loop [i 0]
          (if (< i 3)
            (recur (inc i))
            :success))))
    ))
:success
