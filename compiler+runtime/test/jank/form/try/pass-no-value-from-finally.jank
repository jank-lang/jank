(assert (= (try
             1
             (catch cpp/jank.runtime.object_ref _
               2)
             (finally
               3))
           1))

(assert (= (try
             (throw :anything)
             (catch cpp/jank.runtime.object_ref _
               2)
             (finally
               3))
           2))

:success
