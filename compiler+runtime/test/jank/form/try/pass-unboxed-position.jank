; A box will be required.
(def a (fn* []
         (let* [a 5
                b (try
                    7
                    (catch cpp/jank.runtime.object_ref _
                      ))]
           [])))

:success
