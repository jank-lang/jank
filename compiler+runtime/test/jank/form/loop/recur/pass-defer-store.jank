(def countdown
  (fn* countdown [s]
    (loop* [down s
            up 0]
      (if (cpp/jank.runtime.lt 0 down)
        (recur (cpp/jank.runtime.dec down) (cpp/jank.runtime.inc up))
        [down up]))))

(if (and (= [0 10] (countdown 10))
         (= [0 0] (countdown 0)))
  :success)
