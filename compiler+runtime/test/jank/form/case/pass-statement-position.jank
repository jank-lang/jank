(let [xfn (fn []
            (case 3/4
              4/3 :four-thirds
              3/4 :three-fourths
              :default)
            :arbitrary-value)]
  (assert (= (xfn) :arbitrary-value)))

:success
