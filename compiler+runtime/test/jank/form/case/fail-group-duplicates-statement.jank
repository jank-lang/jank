; failure because keys in a single group must be unique
; test case in statement position
(let [xfn (fn []
            (case 2
              (1 2 2) :one-or-two
              :default)
            :arbitrary-value)])