; This will fail due to duplicate keys
; test case in statement position
(let [xfn (fn []
            (case 1
              1 :one
              1 :duplicate-one
              :default)
            :arbitrary-value)])