(cpp/raw "namespace jank::cpp::literal_value::integer
          {
            bool foo(int) = delete;
          #ifdef WIN32
            bool foo(long long)
          #else
            bool foo(long)
          #endif
            {
              return true;
            }
          }")
(let* [long 2147483648]
  (if (cpp/jank.cpp.literal_value.integer.foo long)
    :success))
