(cpp/raw "namespace jank::cpp::enum_::pass_enum_class
          {
            enum class foo
            {
              bar = 1,
              spam = 10
            };
          }")
(let* [bar cpp/jank.cpp.enum_.pass_enum_class.foo.bar
       spam cpp/jank.cpp.enum_.pass_enum_class.foo.spam]
  (if (and (= 1 bar) (= 10 spam))
    :success))
