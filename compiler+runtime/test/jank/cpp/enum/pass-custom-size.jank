(cpp/raw "namespace jank::cpp::enum_::pass_custom_size
          {
            enum class foo : u8
            {
              bar,
              spam
            };

            struct meow
            {
              foo f1{}, f2{}, f3{}, f4{};
            };
          }")
(let* [bar cpp/jank.cpp.enum_.pass_custom_size.foo.bar
       spam cpp/jank.cpp.enum_.pass_custom_size.foo.spam
       meow (cpp/jank.cpp.enum_.pass_custom_size.meow bar spam bar spam)]
  (if (and (cpp/== bar (cpp/.-f1 meow))
           (cpp/== spam (cpp/.-f2 meow))
           (cpp/== bar (cpp/.-f3 meow))
           (cpp/== spam (cpp/.-f4 meow)))
    :success))
