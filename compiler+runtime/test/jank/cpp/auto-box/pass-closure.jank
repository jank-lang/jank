(let [i (cpp/int. 5)
      ifn (fn* capture-i []
            (assert (= 5 i)))
      _ (ifn)
      f (cpp/float. 5.0)
      ffn (fn* capture-f []
            (if (= 5.0 f)
              :success))]
  (ffn))
