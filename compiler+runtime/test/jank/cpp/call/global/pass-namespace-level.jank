(cpp/raw "namespace jank::cpp::call::global::pass_namespace_level
          {
            void foo()
            {
            }
          }")
(let* [a (cpp/jank.cpp.call.global.pass_namespace_level.foo)]
  (if (nil? a)
    :success))
