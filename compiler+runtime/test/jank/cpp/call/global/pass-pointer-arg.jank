(cpp/raw "namespace jank::cpp::call::global::pass_pointer_arg
          {
            struct bar
            {
              int padding{};
              int data{ -5 };
            };

            int foo(bar *b)
            {
              return b->data;
            }

            bar b{ .data = 50 };
            bar &p{ &b };
          }")
(let* [bar (cpp/new cpp/jank.cpp.call.global.pass_pointer_arg.bar)
       a (cpp/jank.cpp.call.global.pass_pointer_arg.foo bar)
       b (cpp/jank.cpp.call.global.pass_pointer_arg.foo cpp/jank.cpp.call.global.pass_pointer_arg.p)]
  (if (and (= -5 a)
           (= 50 b))
    :success))
