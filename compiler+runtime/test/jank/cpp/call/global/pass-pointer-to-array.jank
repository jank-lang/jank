(cpp/raw "namespace jank::cpp::call::global::pass_pointer_to_array
          {
            std::string foo(char const * const a[])
            {
              std::string ret;
              for(; *a; ++a)
              {
                ret += *a;
              }
              return ret;
            }

            char const *args[3]{ \"Hello \",  \"World\", nullptr };
          }")
(let* [args cpp/jank.cpp.call.global.pass_pointer_to_array.args
       ret (cpp/jank.cpp.call.global.pass_pointer_to_array.foo args)]
  (if (= ret "Hello World")
    :success))
