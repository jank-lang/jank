(cpp/raw "namespace jank::cpp::call::member::pass_ref_qualifier
          {
            struct bar
            {
              int foo(int i) &
              {
                return i;
              }

              int foo(int i) &&
              {
                return i + 1;
              }
            };

            bar b;
            bar &b2{ b };
            bar &&c{ std::move(b) };
          }")
(let* [a (cpp/.foo cpp/jank.cpp.call.member.pass_ref_qualifier.c (cpp/int. 7))
       b (cpp/.foo (cpp/std.move cpp/jank.cpp.call.member.pass_ref_qualifier.c) (cpp/int. 7))]
  (println :a a :b b)
  (if (and (= 7 a) (= 8 b))
    :success))
