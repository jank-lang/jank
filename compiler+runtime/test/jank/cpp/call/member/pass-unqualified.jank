(cpp/raw "namespace jank::cpp::call::member::pass_unqualified
          {
            struct bar
            {
              int foo(int i)
              {
                return i + 1;
              }

              float foo(float i)
              {
                return i;
              }
            };

            int a{ 7 };
          }")
(let* [b (cpp/jank.cpp.call.member.pass_unqualified.bar.)
       a (.foo b cpp/jank.cpp.call.member.pass_unqualified.a)
       f (.foo b (cpp/float. 3.0))]
  (if (and (= 8 a) (= 3.0 f))
    :success))
