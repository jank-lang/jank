(cpp/raw "namespace jank::cpp::call::member::pass_base
          {
            struct foo
            {
              int get_b()
              { return -5; }
            };

            struct bar : foo
            {
              int get_a()
              { return 7; }
            };
          }")
(let* [bar (cpp/jank.cpp.call.member.pass_base.bar.)
       a (cpp/.get_a bar)
       b (cpp/.get_b bar)]
  (if (and (= 7 a) (= -5 b))
    :success))
