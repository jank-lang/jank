(cpp/raw "namespace jank::test::cpp::call::overload::pass_match_with_compatible_types
          {
            int foo(int const a)
            {
              return a;
            }

            struct bar
            {
              bar(int const data)
               : data{ data }
              { }

              int padding{};
              int data{};
            };
            int foo_bar(bar const &b)
            {
              return b.data;
            }
          }")
(let* [f (cpp/float. 5.0)
       ff (cpp/jank.test.cpp.call.overload.pass_match_with_compatible_types.foo f)
       i (cpp/int. 888)
       ii (cpp/jank.test.cpp.call.overload.pass_match_with_compatible_types.foo_bar i)]
  (if (and (= 5 ff) (= 888 ii))
    :success))
