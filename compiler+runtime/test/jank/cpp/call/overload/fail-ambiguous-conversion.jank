(cpp/raw "namespace jank::cpp::call::overload::fail_ambiguous_conversion
          {
            int foo(int const a)
            {
              return a;
            }

            float foo(float const a)
            {
              return a;
            }
          }")
(let* [fi (cpp/jank.cpp.call.overload.fail_ambiguous_conversion.foo 5)]
  :success)
