(cpp/raw "namespace jank::cpp::loop::pass_native_binding_custom_type_no_op_eq
          {
            struct foo
            {
              foo operator +(int d)
              { return { i + d }; }

              foo& operator=(foo const&) = delete;

              int i{};
            };
          }")
(let* [r (loop [it (cpp/jank.cpp.loop.pass_native_binding_custom_type_no_op_eq.foo)]
           (if (cpp/== #cpp 3 (cpp/.-i it))
             (cpp/.-i it)
             (recur (cpp/+ it #cpp 1))))]
  (if (= 3 r)
    :success))
