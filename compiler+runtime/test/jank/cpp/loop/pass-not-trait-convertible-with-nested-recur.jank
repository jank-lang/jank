(cpp/raw "namespace jank::cpp::loop::pass_not_trait_convertible_with_nested_recur
          {
            struct foo
            { i64 data{}; };
          }")
(let* [foo1 (loop* []
              (if true
                (cpp/jank.cpp.loop.pass_not_trait_convertible_with_nested_recur.foo #cpp 1)
                (do
                  (do
                    (recur)))))
       foo2 (loop* []
              (if false
                (do
                  (do
                    (recur)))
                (cpp/jank.cpp.loop.pass_not_trait_convertible_with_nested_recur.foo #cpp 2)))]
  (if (and (= 1 (.-data foo1))
           (= 2 (.-data foo2)))
    :success))
