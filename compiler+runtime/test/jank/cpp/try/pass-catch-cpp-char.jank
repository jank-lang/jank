;; Test catching C++ char exception
(cpp/raw "void throw_char_z() { throw 'z'; }")

(assert
 (= \z
    (try
      (cpp/throw_char_z)
      (catch cpp/int i
        (println "Wrong: caught int")
        \0)
      (catch cpp/char c c))))

:success
