(cpp/raw "namespace jank::cpp::member::pass_access_base
          {
            struct foo
            {
              int b{ -5 };
            };

            struct bar : foo
            {
              int a{};
            };
          }")
(let* [b (cpp/jank.cpp.member.pass_access_base.bar.)
       b' (cpp/.-b b)]
  (if (= -5 b')
    :success))
