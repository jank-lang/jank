(cpp/raw "namespace jank::cpp::member::pass_access_unqualified
          {
            struct bar
            {
              int a{};
            };
          }")
(let* [b (cpp/jank.cpp.member.pass_access_unqualified.bar. #cpp 77)
       a (.-a b)]
  (if (= 77 a)
    :success))
