(cpp/raw "namespace jank::cpp::member::pass_access_base_template
          {
            template <typename T>
            struct foo
            {
              T b{ -5 };
            };

            template <typename T>
            struct bar : foo<T>
            {
              T a{ 7 };
            };

            template <typename T>
            bar<T> meow()
            { return {}; }
          }")
(let* [meow (cpp/value "&jank::cpp::member::pass_access_base_template::meow<int>")
       bar (meow)
       a (cpp/.-a bar)
       b (cpp/.-b bar)]
  (if (and (= 7 a) (= -5 b))
    :success))
