(cpp/raw "namespace jank::cpp::member::pass_access
          {
            struct bar
            {
              int a{};
            };
          }")
(let* [b (cpp/jank.cpp.member.pass_access.bar.)
       a (cpp/.-a b)]
  (if (= 0 a)
    :success))
