(let* [i (cpp/int. 2)
       l (cpp/long. 5)
       s (cpp/short. 3)
       _ (assert (= false (cpp/> i i)))
       _ (assert (= true (cpp/> s i)))
       _ (assert (= false (cpp/> i l)))]
  :success)
