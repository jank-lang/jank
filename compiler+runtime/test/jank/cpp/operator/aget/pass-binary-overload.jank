(cpp/raw "namespace jank::cpp::operator_::aget::pass_binary_overload
          {
            struct foo
            {
              int& operator [](int const i)
              {
                return arr[i];
              }

              /* Not used. Ensures we're offsetting fields correctly. */
              int padding{};
              int arr[3]{ 100, 200, 300 };
            };
          }")
(let* [i0 (cpp/int.)
       l2 (cpp/long. 2)
       foo (cpp/jank.cpp.operator_.aget.pass_binary_overload.foo.)
       a0 (cpp/aget foo i0)
       _ (assert (= 100 a0))
       a2 (cpp/aget foo l2)
       _ (assert (= 300 a2))]
  :success)
