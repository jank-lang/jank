(cpp/raw "namespace jank::cpp::operator_::aget::fail_binary_no_subscript
          {
            struct foo
            {
              /* Not used. Ensures we're offsetting fields correctly. */
              int padding{};
              int arr[3]{ 100, 200, 300 };
            };
          }")
(let* [foo (cpp/jank.cpp.operator_.aget.fail_binary_no_subscript.foo)
       a0 (cpp/aget foo (cpp/int))]
  :success)
