(let* [i (cpp/int. 2)
       l (cpp/long. 5)
       s (cpp/short. 3)
       _ (assert (= true (cpp/<= i i)))
       _ (assert (= true (cpp/<= i s)))
       _ (assert (= false (cpp/<= l i)))]
  :success)
