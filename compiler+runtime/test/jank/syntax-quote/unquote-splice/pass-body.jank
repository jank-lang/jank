(def result (atom ""))

(defmacro do-build-keyword [& body]
  `(do
     ~@body))

(assert (= 42 (do-build-keyword
                (swap! result str "s")
                (swap! result str "uccess")
                (swap! result keyword)
                42)))

; Should be :success.
@result
