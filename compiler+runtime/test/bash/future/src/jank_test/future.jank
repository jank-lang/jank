(ns jank-test.future)

(defn -main []
  (dotimes [_ 100000]
    (let [atm (atom [])
          a (future (swap! atm conj (gensym "a")))
          b (future (swap! atm conj (gensym "b")))]
      @a
      @b
      (prn @atm "DONE"))))
