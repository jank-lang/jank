(ns clojure-test.self-test
  (:require [clojure.test :as t]))

(t/deftest a-test (is (= 1 2)))

(defn -main []
  (assert (= {:test 1, :pass 0, :fail 1, :error 0, :type :summary}
             (clojure.test/run-test-var #'a-test)))
  :success)
