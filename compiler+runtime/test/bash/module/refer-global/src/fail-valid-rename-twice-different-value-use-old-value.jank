(ns test
  (:refer-global :only [std.string std.string.npos]
                 :rename {std.string string
                          std.string.npos foo}))

(ns test
  (:refer-global :only [std.string std.string.npos]
                 :rename {std.string my-string
                          std.string.npos foo}))

(let [s (string #cpp "success")]
  (println :success))
