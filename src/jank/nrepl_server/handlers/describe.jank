(ns nrepl-server.handlers.describe)

(defn handle [_]
  {:aux      {}
   :ops      {:clone     {}
              :describe  {}
              :eval      {}
              :load-file {}}
   :versions {}
   :status   '(:done)})
