; Basic definition.
(def one 1)
(assert (= one 1))

; Definition with another var does a deref.
(def one-again one)
(assert (= one-again one))

; Definition with a more complex expression.
(def two (+ one one-again))
(assert (= two 2))

; Redefinition.
(def one 100)
(assert (= one 100))
(assert (= one-again 1))

; Nested definition, where the outer var holds the inner var.
(def nested (def odd? true))
(assert odd?)
; TODO: Ability to assert this.
;(assert (deref nested))

; TODO: Return the constant to ensure the test ran fully.
; "jank"
