(function overloaded () ()
  (print "nullary"))

(function overloaded (d integer) ()
  (print "integer"))

(function overloaded (d integer dd real) ()
  (print "integer and real"))

(overloaded 42 3.14)
(overloaded -77)
(overloaded)
