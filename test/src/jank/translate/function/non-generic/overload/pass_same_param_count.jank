(function overloaded (d integer) ()
  (print "integer"))

(function overloaded (d real) ()
  (print "real"))

(function overloaded (d string dd boolean) ()
  (print "string and boolean"))

(function overloaded (d boolean dd string) ()
  (print "boolean and string"))

(overloaded 42)
(overloaded 42.0)

(overloaded "foo" false)
(overloaded true "bar")
