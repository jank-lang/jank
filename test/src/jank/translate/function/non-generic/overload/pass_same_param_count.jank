(ƒ overloaded (d integer) ()
  (print "integer"))

(ƒ overloaded (d real) ()
  (print "real"))

(ƒ overloaded (d string dd boolean) ()
  (print "string and boolean"))

(ƒ overloaded (d boolean dd string) ()
  (print "boolean and string"))

(overloaded 42)
(overloaded 42.0)

(overloaded "foo" false)
(overloaded true "bar")
