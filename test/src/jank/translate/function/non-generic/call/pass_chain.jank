(function test-integer (d integer) ()
  (print d))

(function test-real (d real) ()
  (print d))

(function test-string (d string) ()
  (print d))

(function test (a integer b real c string) ()
  (test-integer a)
  (test-real b)
  (test-string c))

(test 77 -5.05 "good")
