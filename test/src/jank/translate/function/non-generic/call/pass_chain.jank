(ƒ test-integer (d integer) ()
  (print d))

(ƒ test-real (d real) ()
  (print d))

(ƒ test-string (d string) ()
  (print d))

(ƒ test (a integer b real c string) ()
  (test-integer a)
  (test-real b)
  (test-string c))

(test 77 -5.05 "good")
